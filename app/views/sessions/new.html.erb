<div class= "backdrop">
<nav class="navbar navbar-default">
  
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>

      </button>
      <a class="navbar-brand" href="/">Git To The Reef</a>
    </div>
   <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
       
        <li><%= link_to  "Play Game", level_one_path %></li>
   
        
        <li><% if current_user %>
          <%= link_to "#{current_user.name.split.first}", user_profile_path(current_user) %>
          <% else %> 
          <% end %>
        </li> 




        <li class="dropdown">
          <a class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><span class="glyphicon glyphicon-cog" aria-hidden="true"> </span><span class="caret"></span>
          <ul class="dropdown-menu" role="menu">
            
            <li><% if current_user %>
            <%= link_to "Edit Account", edit_users_path(current_user) %>
          <% else %>
              
          <% end %></li>  


            <li>
                <% if current_user %>
              <%= link_to "Log out", destroy_session_path %>
          <% else %>
              <%= link_to "Login", new_session_path %>
          <% end %></li>

          <li><% if current_user %>
            
          <% else %>
               <li><%= link_to "Create New Account", new_user_path %></li>
          <% end %></li> 
           
          
          </ul>
        </li>
      </ul>
  </div>
   
  </nav>


<div class = "container">

<% flash.each do |key, value| %>
    <h4><%= value %></h4>
<% end %>
  <body>
<div class="landingpic"></div>
  <%= form_for @user, url: create_session_path do |f| %>

    <div class="form-group">
    <%= f.label :email %>
    <%= f.email_field :email, class: "form-control" %>
    </div>

    <div class="form-group">
    <%= f.label :password %>
    <%= f.password_field :password, class: "form-control" %>
    </div>
    
    <%= f.submit "Log In", class: "submit-button-create" %>
    <%= link_to "Create New Account", new_user_path, class: "submit-button" %>
<% end %>
  <div class="Title"> GIT TO THE REEF</div>

</body>
</div>
